# P2C-04 Evidence - AIWS Server and Runtime

Status date: 2026-08-18

P2C-04 is verified against the real `E:\AIWorkflowStudio` workspace.

## Implementation

- `server` uses the Maven Runner in `aiws-server` and receives its HTTP port, PostgreSQL URL, OIDC issuer, allowed Web origin, Agent Runtime URL, MinIO endpoint, ClamAV port, OTLP endpoint, and Secret references only through the resolved manifest environment.
- `agent-runtime` uses the existing workspace Python virtual environment and receives its loopback port, internal token, and credential references through the same resolved spec boundary.
- Both services depend on `keycloak-configure: completed`; neither is released by a merely started or failed oneshot.
- Server readiness is the real `/actuator/health/readiness` endpoint. Runtime readiness is the real `/health/ready` endpoint. No fixed startup delay or original PowerShell launcher is used.

## Real Verification

`scripts/verify-p2c-07.ps1` started the complete real DAG. The resulting status contained five services and thirteen planned ports. The Server and Runtime reached `ready` with real managed process identities only after Infrastructure and Keycloak Configure succeeded.

```text
server        process / daemon  ready  127.0.0.1:18080
agent-runtime process / daemon  ready  127.0.0.1:18090
```

Both readiness endpoints returned success before the start Operation completed. The same services were recovered after the StackPilot control plane was terminated and recreated against the same isolated data directory and instance identity.

