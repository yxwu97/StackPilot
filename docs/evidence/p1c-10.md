# P1C-10 Operation Center Evidence

Status: verified with a real browser on 2026-08-18.

- The operation center lists persisted Operations newest-first with system, terminal/active state, completed step count, and creation time.
- The detail drawer reloads the selected Operation and renders each persisted step, duration, status, and safe error code. Cancellable queued/running Operations expose cancellation.
- Real start Operation `op_01M08SSBMH6JW3FEJ9NCQRQP6F` displayed `10 / 10`; its timeline showed Backend readiness before Web start and the final aggregate step.
- Repository tests cover workspace filtering, newest-first ordering, and bounded list size; API tests cover list/detail/cancel routes and safe DTO mapping.

Verified by `go test ./internal/storage ./internal/api -count=1` and Playwright CLI session `stackpilot-p1c`.
