# P1C-09 Service Detail Web Evidence

Status: verified with a real browser on 2026-08-18.

- The service drawer shows state, PID, service-instance ID, dependencies, start time, command digest, restart, log filter, pause/resume, and UTF-8 download controls.
- SSE parsing rejects malformed payloads; pause keeps the stream bounded and merges deduplicated sequence entries on resume.
- The client cache is capped at 5,000 entries. The fixed-height window renders only the visible range plus overscan instead of mounting the complete cache.
- Real Web logs filtered from eight entries to the two `VITE` matches. Pause changed the accessible action to resume, and download produced UTF-8 `web.log` with the two filtered full messages.
- Real Backend drawer rendered 44 visible rows without layout shift; full messages remain present in the download and hover title.

Verified by `npm run type-check`, `npm run build:web`, Playwright CLI interaction, and `go test ./internal/logs ./internal/api -count=1`.
