# P1D-07 CLI MVP Evidence

- `internal/cli` is an authenticated REST/SSE client; `cmd/stackpilot` only registers the command surface.
- Implemented `workspace add`, `up`, `down`, `status`, `logs [--follow]`, `wait`, and `open`, with current-workspace ancestor discovery and explicit `--server`/`--data-dir` isolation.
- `--output table|json` keeps JSON stdout machine-readable; wait progress uses stderr. Exit codes distinguish arguments/configuration, availability, Operation failure, authentication, and conflict.
- Wait uses durable domain-event SSE and falls back to polling; Ctrl+C stops waiting without cancelling the Operation. Log follow uses authenticated SSE with stable sequence cursors.
- Redirects are refused so Bearer credentials cannot leave the selected literal-loopback origin. Unit tests cover headers, idempotency, errors, redirects, argument ordering, workspace discovery, and SSE wait.
- Real BTC CLI Gate on `127.0.0.1:32104`:
  - start `op_01M090AJMX5FPX6YCA0H4SWETQ` succeeded with all 10 steps;
  - `status` reported Backend/Web ready on ports 8081/32102;
  - Backend logs returned one valid JSON array with sequences 1 through 62;
  - stop `op_01M090CSJQ438Y3X6S0NK74X02` succeeded in reverse dependency order;
  - ports 8081, 32102, and 32104 had no listeners after cleanup.

