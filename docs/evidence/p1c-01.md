# P1C-01 DAG Orchestration Evidence

Status: verified on Windows on 2026-08-18.

- The orchestrator validates dependency references and cycles, produces stable topological layers, and derives reverse layers for stop.
- System start releases a service only after every dependency is ready. System stop traverses the graph in reverse and keeps independent work bounded.
- Integration coverage verifies Backend starts before Web, Web remains unstarted after Backend readiness failure, and stop order is Web then Backend.
- Real BTC start Operation `op_01M08TDZ7FEVZ3QKR57TTSR0TS` recorded `wait-ready:backend` completing in 18,045 ms before `start:web`; Web then reached ready in 1,863 ms.
- Real BTC stop Operation `op_01M08TJKCVP2HPSV46JERESD1A` stopped Web before Backend and completed successfully.

Verified by `go test ./internal/orchestrator -count=1` and the final `scripts/check.ps1` run outside the restricted sandbox.
