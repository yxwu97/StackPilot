# P1C-04 Resolved System Specification Evidence

Status: verified on 2026-08-18.

- `ResolvedSystemSpec` freezes the manifest digest, DAG topology, failure policy, resolved runners, service environment, readiness configuration, stop timeout, log spool paths, and complete port plan.
- Approved port references are expanded from the one plan into Backend port, Web listen port, proxy target, CORS origin, readiness targets, and access URLs.
- Resolved specifications are hashed, persisted, and linked from system instances; service restart refuses a changed manifest digest.
- Real instance `si_01M08TDZ7M8WGN0HDK74SACF4C` persisted resolved digest `b467b55cb6...86324a` and successfully propagated its two selected ports through both services.

Verified by `go test ./internal/orchestrator ./internal/storage ./internal/manifest -count=1`.
