# P1D-08 VSCode Tasks Evidence

- `docs/examples/vscode/tasks.json` provides process tasks for current-workspace start/wait/open, stop/wait, status, and authenticated Web open.
- Every task uses `${workspaceFolder}` as cwd and delegates to the public CLI/API path; no task embeds commands from a manifest, token material, business scripts, or a second lifecycle implementation.
- PowerShell `ConvertFrom-Json` validation passed. No task or runtime configuration uses port 5173.

