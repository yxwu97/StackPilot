# P2E-01 Evidence - Liveness

Status date: 2026-08-19

The liveness monitor owns one bounded worker per eligible daemon, replaces the worker when the resolved check changes, records each result, applies consecutive success/failure thresholds, and restores `ready` after recovery. Old detailed health rows compact into hourly aggregates.

Verified by `TestEnsureRuntimeLivenessKeepsMatchingMonitorAndReplacesChangedSpec`, the health repository success/invariant/foreign-key tests, and `TestHealthResultRepositoryCompactsBoundedOldDetails`. `go test ./internal/orchestrator` and the relevant storage package tests passed.
