# P1A-03 Semantic Validator Evidence

- Applies documented defaults without mutating the Loader document.
- Validates system/service/port identifiers, fallback ranges, service DAG references and cycles.
- Restricts templates to documented ports, workspace, instance, system, and gated Secret tokens.
- Rejects Phase 2 compose, oneshot/completed, Python venv, liveness, auto-restart, and Secret capabilities.
- Restricts TCP/HTTP readiness to loopback and validates health/policy duration relationships.
- Resolves Windows junction targets with `GetFinalPathNameByHandle`; a real junction escape test passes without requiring symlink privilege.

Verified with `go test ./internal/manifest -count=1 -v` and `go vet ./internal/manifest`.
