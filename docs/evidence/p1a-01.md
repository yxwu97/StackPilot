# P1A-01 Workspace Registration Evidence

- Fixed discovery path: `.stackpilot/system.yaml`; callers cannot supply another manifest filename.
- Workspace roots and fixed files are canonicalized before access. Windows uses final paths from file handles so directory junctions cannot escape the registered root.
- Workspace IDs are canonical `ws_`-prefixed ULIDs generated from UTC milliseconds and cryptographic entropy.
- Registration and unregistration never write or delete workspace project files.
- Real SQLite tests cover initial registration, duplicate canonical paths, stable listing, and catalog-only deletion.

Verified with `go test ./internal/storage ./internal/api -count=1`.
