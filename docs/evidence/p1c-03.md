# P1C-03 Port Planning Evidence

Status: verified on Windows on 2026-08-18.

- Port planning precedence is request override, workspace override, sticky history, preferred, then fallback for `auto`; `strict` and `override-only` have separate tested candidate sets.
- Planning probes the complete set before persisting leases. SQLite uniqueness protects active port ownership, and probe/lease races return `PORT_CONFLICT` without locally rewriting one service.
- Migration `000007` adds immutable plan assignments, active leases, and sticky history; repository tests cover release and ownership constraints.
- Unit tests cover preferred, occupied preferred, fallback, sticky reuse, explicit overrides, strict, override-only, multi-port atomic retry, and exhaustion.
- Real BTC instance `si_01M08SSBN6TSYFT33Y4RRT6JXP` observed Backend preferred `8081` replaced by fallback `8200`, while Web reused sticky `32102`; both readiness checks passed.

Verified by `go test ./internal/ports ./internal/storage ./internal/orchestrator -count=1` and the real listener/readiness checks.
