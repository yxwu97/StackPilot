# P1D-02 Log Resume Evidence

Status: verified on Windows on 2026-08-18.

- Every persisted NDJSON line records an internal source-spool end offset while the public log DTO remains unchanged.
- Resume scans registered and unregistered final segments, repairs `.ndjson.active` files by truncating incomplete tails, atomically publishes them, and registers metadata idempotently.
- Capture resumes from per-stream durable byte offsets and the greatest persisted sequence. Legacy segments without offsets skip already-existing spool bytes instead of duplicating historical logs.
- Tests cover graceful restart, logs appended while the control plane is down, incomplete active tails, final files renamed before index commit, and repeated recovery.
- The real BTC forced-restart Gate preserved Backend sequences `1..62` and Web sequences `1..8`; every sequence was unique, no historical record was replayed, and no crash `.active` segment remained unfinalized before resumed capture.

Verification: `go test ./internal/logs ./internal/orchestrator ./internal/storage -count=1` and the full repository check passed.
