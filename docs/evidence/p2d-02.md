# P2D-02 Evidence - PMS Backend

Status date: 2026-08-18

The real PMS Backend now reads `SERVER_PORT` with an 8080 standalone default. Spring Boot Actuator exposes only the health endpoint, details are hidden, and the security chain permits the exact `/actuator/health` paths required by local readiness.

The managed manifest propagates the planned Backend and RAG ports through `SERVER_PORT` and `PM_KNOWLEDGE_RAG_BASE_URL`. The shared RAG token is a StackPilot Secret reference, and the business application's configurable RAG restart command is explicitly disabled in the managed environment so lifecycle ownership stays with StackPilot.

Verification:

```text
mvn -DskipTests compile
BUILD SUCCESS

real Gate Backend endpoint
http://127.0.0.1:8080/actuator/health -> status UP
```

The real Gate connected the existing MySQL and Redis services and reached Backend `ready` without a fixed delay.
