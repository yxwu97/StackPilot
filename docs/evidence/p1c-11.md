# P1C-11 Full BTC Integration Evidence

Status: verified on Windows on 2026-08-18.

Workspace and runtime:

- Workspace: `E:\BidTravelCloud`
- Workspace ID: `ws_01M08RGPZJE3GA34Z9XZFFKP7Q`
- Manifest digest: `68da5f795e6d9a47894d3cb1f46af7b93c1f089c7dc705c90a3094cb3b4b1851`
- StackPilot verification URL: `http://127.0.0.1:32103` (no service uses `5173`)

Real Gate results:

- Start `op_01M08TDZ7FEVZ3QKR57TTSR0TS` succeeded with all 10 steps. Backend became ready before Web started; Web then became ready.
- Backend health returned HTTP 200/UP and BTC Web returned HTTP 200. Resolved ports propagated to listen, readiness, proxy, and access paths.
- A separate real start `op_01M08SSBMH6JW3FEJ9NCQRQP6F` exercised preferred-port conflict: Backend used fallback `8200` after `8081`, while Web reused sticky `32102`.
- Web-only restart preserved Backend. Backend restart applied the downstream Web closure. Both restart Operations succeeded.
- Stop `op_01M08TJKCVP2HPSV46JERESD1A` stopped Web before Backend, succeeded, removed `8200/32102` listeners, and left no BTC wrapper process.
- Web start, progress, logs, service restart controls, operation timeline, overview synchronization, and reverse stop were exercised in a real Chromium session.

Final automated verification:

- `scripts/check.ps1` outside the restricted sandbox: Web type-check/build, all Go tests including Windows Named Pipe/Job Object, and `go vet` passed.
- `git diff --check` passed.
- `AGENTS.md` and `CLAUDE.md` SHA-256 both equal `4F4400A0BBCC542096D6DC7A31B54EFEF351D3E3C932D7126A17F5CAA69456B1`.

Known limitation: Maven/JVM receives the currently approved process-group `CTRL_BREAK`, emits a thread dump, and requires the 15-second Job Object fallback. The complete tree is terminated and the port is released, but a different graceful signal strategy requires a Supervisor design/ADR revision and is not claimed here.
