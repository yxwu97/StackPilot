# P1C-06 Service Restart Evidence

Status: verified on Windows on 2026-08-18.

- Service restart computes the target's downstream closure from the persisted resolved specification and rejects a refreshed manifest digest mismatch.
- Restarting Web Operation `op_01M08RVH58YPPSZFJG12H2FMC2` ran only validate, stop/start/wait Web, and aggregate; Backend PID stayed unchanged.
- Restarting Backend Operation `op_01M08RYV77YX6Z65513Q6FX1YF` stopped Web then Backend, waited for Backend readiness, and then started Web. Both service PIDs changed and selected ports remained stable.
- Automated tests cover leaf restart, dependency restart with downstream closure, invalid targets, missing runtimes, digest conflict, and stop/start failure aggregation.

Verified by `go test ./internal/orchestrator ./internal/api -count=1` and the two real BTC restart Operations above.
