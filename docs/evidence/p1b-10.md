# P1B-10 Process Fixture Evidence

- `test/fixtures/process-fixture` is a standalone Go process program with bounded, deterministic modes for `slow-ready`, `immediate-exit`, `child-tree`, `ignore-terminate`, `large-log`, and `hold-port`.
- Slow readiness delays before binding a loopback TCP listener, allowing immediate-check and timeout scheduling to be tested without sleeps in production code.
- Immediate exit uses a caller-selected exit code. Child-tree writes the child PID to a marker, and both the parent and child-worker ignore cooperative termination so Job Object fallback can be asserted.
- Large-log writes independently bounded stdout and stderr volumes before remaining alive. Port competition starts two listeners on the same loopback port and produces a deterministic bind failure.
- The fixture self-test builds the standalone executable and verifies all six behaviors. Existing Windows Supervisor/Process Driver integration tests continue to verify graceful/forced whole-tree termination, identity mismatch refusal, reconnect, and crash kill-on-close using the same regression run.

Verified with:

- `go test ./test/fixtures/process-fixture -count=1 -v`
- `go test ./... -count=1` outside the restricted sandbox

These fixtures are repeatable internal evidence only. They do not satisfy or replace the blocked BTC Backend real-integration Gate.
