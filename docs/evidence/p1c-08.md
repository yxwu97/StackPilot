# P1C-08 System Detail Web Evidence

Status: verified with a real browser on 2026-08-18.

- The system detail presents server runtime state, immutable manifest/resolved digests, readiness count, active Operation, dependency-aware service status, selected ports, port source, and replacement details.
- Action availability follows server state and active Operation state. Completing a mutation refreshes both runtime and catalog REST snapshots, so returning to the overview shows the current state.
- The final browser run changed the overview from `stopped / 0 of 2` to `running / 2 of 2` after start without a manual reload.
- Desktop and 390x844 mobile screenshots were visually checked for overflow and overlap:
  - `output/playwright/p1c-system-stopped-desktop.png`
  - `output/playwright/p1c-system-stopped-mobile.png`
  - `output/playwright/p1c-system-running-desktop.png`

Verified by `npm run type-check`, `npm run build:web`, Playwright CLI session `stackpilot-p1c`, and a browser console with zero errors.
