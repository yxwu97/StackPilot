# P1D-10 Windows Installation and Upgrade Evidence

- D-04 is closed by `docs/adr/0003-windows-user-background-task.md`. Phase 1 uses a no-elevation current-user background process, HKCU login startup registration, and a current-user/SYSTEM ACL Named Pipe; it does not run user projects as LocalSystem.
- `stackpilot service install|upgrade|start|stop|status|uninstall` uses a strict installation marker and immutable `versions/<sha256>/stackpilot.exe` paths. Install and data roots must not overlap. Every removal revalidates the marker, executable checksum, canonical root, and installation ID.
- Unit tests passed for fresh/idempotent install, upgrade, data-preserving uninstall, overlapping-root rejection, executable tamper rejection, Windows command-line quoting, and the control Pipe. The real Pipe test passed outside the restricted sandbox.
- A real isolated upgrade on port `32106` changed the running control plane from PID `3080` to PID `43808`, retained the workspace ID, SQLite state, logs, and `p1d10-preserve.txt`, and recorded graceful stop/start without unverified process termination.
- External-candidate uninstall and installed-command self-uninstall both passed. The final candidate `0.1.0-p1d10` started as PID `22428`, returned `ready`, and self-uninstalled to `not-installed`; the verified installation root, HKCU value, PID, listener, cleanup script, and tombstone were absent afterward. `uninstall-cleanup.log` contained `completed`, while `.local/p1d10-data`, `stackpilot.db`, and the preservation sentinel remained intact.
- GoReleaser v2.12.7 validated `.goreleaser.yml` and built `stackpilot_0.0.0-SNAPSHOT-b47ab22_windows_amd64.zip`. Its SHA-256 `9f5621e72b60371f734abe9b664ebc9827e846902e28dd2954456fd6b65925ad` matched `checksums.txt`; the archive contained only `README.md` and `stackpilot.exe`. The executable reported version `0.0.0-SNAPSHOT-b47ab22`, commit `b47ab22f97c765a06b4b4cee51531abc50a037e5`, and UTC build time `2026-08-18T02:08:14Z`.
- CI creates the same GoReleaser snapshot artifact, and the tag-only release workflow runs the full repository check before publishing with read/write scope limited to repository contents. Contract tests pin GoReleaser v2.12.7, Windows amd64 ZIP output, all three metadata fields, and SHA-256 checksums.
- `scripts/check.ps1` passed Web tests 6/6, TypeScript checks, the production Web build, all Go tests, and `go vet ./...`. An AST-based check found no production Go function over 50 lines. No service or development configuration uses port `5173`.

