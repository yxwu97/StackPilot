# P2E-04 Evidence - IncidentContext

Status date: 2026-08-19

IncidentContext uses explicit time, line, byte, and evidence budgets. It references persisted event IDs, health result IDs, and service-instance log sequences; adjacent duplicate lines are folded and all log text is redacted before persistence.

`TestBuildContextRedactsBoundsAndFoldsLogs`, `TestReportServiceIncidentBuildsBoundedRedactedLogEvidence`, Incident repository round-trips, and API bounded-evidence tests passed. Context DTOs contain no command, environment, token, Secret value, or internal process identity.
