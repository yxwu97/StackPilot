# P1C-05 System Operation Evidence

Status: verified on Windows on 2026-08-18.

- Start, stop, and restart are persisted asynchronous Operations with database workspace locking, idempotency, ordered steps, terminal aggregation, and stable error codes.
- Cancellation is durable before worker cancellation and converges successfully cleaned resources to stopped; cleanup failure produces a failed Operation.
- Port allocation is retried as a complete plan and never mutates an already-created instance specification.
- API routes return `202` Operation references and preserve `409` activity conflicts and idempotent replay behavior.
- Final real start `op_01M08TDZ7FEVZ3QKR57TTSR0TS` completed all 10 steps in 20,880 ms. Final stop `op_01M08TJKCVP2HPSV46JERESD1A` completed all 4 steps and left no BTC listener.

Verified by `go test ./internal/orchestrator ./internal/api ./internal/storage -count=1` and real REST/Web execution.
