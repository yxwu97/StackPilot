# P2D-03 Evidence - PMS RAG

Status date: 2026-08-18

The real PMS RAG service runs through the checked-in `.venv` using the `python-venv` Runner. `RAG_HOST`, `RAG_PORT`, Qdrant URL, shared service token, and provider key are injected from the immutable resolved specification; sensitive values use StackPilot Secret references.

`/health` now returns HTTP 503 with `status=degraded` when Qdrant cannot be reached and HTTP 200 only with `status=ok` and `qdrant=true`. This makes the existing StackPilot HTTP readiness contract authoritative rather than accepting a degraded body as ready.

Verification covered Python syntax parsing plus the real Qdrant-backed Gate on planned port 8100. RAG reached `ready`, produced managed logs, stopped cleanly, and restarted on the same sticky port.
