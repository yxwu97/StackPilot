# P1A-08 BTC Configuration Audit

Status: verified on 2026-08-18.

The BTC integration audit identified and resolved these blockers in `E:\BidTravelCloud`:

- Backend reads `SERVER_PORT`, exposes only Actuator health, and permits unauthenticated `/actuator/health` without exposing health details.
- Backend CORS reads an exact comma-separated `BIDTRAVEL_CORS_ALLOWED_ORIGINS` value instead of allowing wildcard origins with credentials.
- Vite defaults to port `32102`, rejects invalid `VITE_PORT`, honors the Runner `--port`, and reads `VITE_API_TARGET` for its API proxy.
- The checked-in `.stackpilot/system.yaml` propagates planned Backend/Web ports to `SERVER_PORT`, CORS, Vite proxy, and readiness.

Verification included `mvn -DskipTests compile`, the BTC Web production build, a StackPilot-managed real Backend start on port `8081`, `UP` health, allowed Origin `http://127.0.0.1:32102`, rejected Origin `http://127.0.0.1:5173`, and managed stop with port/process release.
