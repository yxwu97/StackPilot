# P1C-07 npm and Node Runtime Evidence

Status: verified on Windows on 2026-08-18.

- npm resolution uses the trusted Runner allowlist, canonical executable paths, fixed `%COMSPEC% /d /s /c` invocation, array arguments, and bounded version preflight.
- BTC Web starts through its declared npm Runner, binds exact loopback port `32102`, consumes the resolved Backend proxy target, persists stdout/stderr, and remains inside the Supervisor Job tree.
- Real Web readiness returned HTTP 200 and logs recorded `vite --host 127.0.0.1 --port 32102` followed by the ready URL.
- Stop removed the Web listener and complete process tree. Windows OEM output is decoded before redaction/persistence; the real localized prompt is stored as valid UTF-8 `终止批处理操作吗(Y/N)?`.

Verified by `go test ./internal/runner ./internal/driver/process ./internal/platform/windows/supervisor ./internal/logs -count=1` and real BTC Web lifecycle checks.
