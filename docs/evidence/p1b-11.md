# P1B-11 BTC Backend Integration Evidence

Status: verified on Windows on 2026-08-18.

- BTC Backend now reads `SERVER_PORT`, exposes unauthenticated `/actuator/health`, and limits Actuator exposure to health without details.
- BTC CORS uses the exact injected `BIDTRAVEL_CORS_ALLOWED_ORIGINS`; `http://127.0.0.1:32102` was accepted and `http://127.0.0.1:5173` was rejected with HTTP 403.
- BTC Web defaults to `32102`, validates `VITE_PORT`, and reads `VITE_API_TARGET`; its production build passed.
- `E:\BidTravelCloud\.stackpilot\system.yaml` registered as workspace `ws_01M08MNCVR69CDMY2Y7TPX8WGH` with valid digest `68da5f795e6d9a47894d3cb1f46af7b93c1f089c7dc705c90a3094cb3b4b1851`.
- Start Operation `op_01M08MNMH11JXBG17T8C3RYZRK` succeeded in 18,688 ms. Backend reached `ready`, Actuator returned `UP`, and captured logs retained the Operation ID and monotonic sequence.
- Stop Operation `op_01M08MPR6KZT4V57MZ2TB4N2H4` succeeded in 15,023 ms after the configured graceful window and forced Job fallback.
- After stop, runtime status was `stopped`, port `8081` was released, the Supervisor identity file was removed, and neither the managed root PID nor the instance Supervisor remained.

The first stop attempt exposed a client protocol deadline defect: a 15-second graceful timeout used the same 15-second client I/O deadline. The fix adds the graceful duration to the protocol deadline, closes an empty Supervisor after stop, maps failures to `PROCESS_STOP_FAILED`, and converges unexecuted terminal steps to `skipped`. Windows regression tests and the repeated real BTC Gate passed after the fix.

Commands and checks:

- `mvn -DskipTests compile` in `E:\BidTravelCloud\bidtravel-backend`
- `npm run build` in `E:\BidTravelCloud\bidtravel-frontend`
- StackPilot REST workspace registration, start/status/Operation/log/stop requests on `127.0.0.1:32100`
- HTTP health and CORS preflight checks on `127.0.0.1:8081`
- Windows listener, process identity, Supervisor identity-file, and residual-process checks
