# P2E-05 Evidence - Deterministic Rules

Status date: 2026-08-19

The rule engine covers port conflict, daemon exit, readiness refusal/timeout, HTTP mismatch, dependency state, and known Java `BindException`, Node `EADDRINUSE`, and Python `ModuleNotFoundError` signatures. Results cite evidence and every suggestion has `automatic=false`.

`TestRuleEngineProducesTraceableNonAutomaticResults` passed. The full fault matrix in `TestPhase2EFaultDiagnosticsPersistTraceableNonAutomaticAnalyses` passed ten consecutive runs against real SQLite and controlled process/network fixtures, then passed under the race detector with the API, Incident, orchestrator, and storage packages.
