# P2C-05 Evidence - AIWS Web and OIDC Propagation

Status date: 2026-08-18

P2C-05 is verified against the real `E:\AIWorkflowStudio` workspace.

## Implementation

- The AIWS Web service uses the npm Runner with `--host 127.0.0.1`, the resolved `${ports.web}`, and `--strictPort`. Its preferred port remains the real AIWS baseline `6173`; port `5173` is not used.
- AIWS Vite now reads `VITE_API_TARGET`, so the proxy target follows `${ports.server}` instead of a hard-coded Backend address.
- The resolved Web environment uses one port plan for API proxy, OIDC authority, callback URI, post-logout URI, Keycloak allowed origin, and Server CORS origin.
- Web depends on both Server and Agent Runtime readiness. Its real HTTP root is the final readiness check and the StackPilot UI entry target.

## Real Verification

The complete Gate resolved and exercised these endpoints from the same plan:

```text
Web           http://127.0.0.1:6173
Server        http://127.0.0.1:18080
OIDC issuer   http://127.0.0.1:18180/realms/aiws
Runtime       http://127.0.0.1:18090
```

The Server readiness endpoint, Runtime readiness endpoint, Web root, and Keycloak discovery document all returned success. The authenticated StackPilot browser view exposed `http://127.0.0.1:6173` as the `打开 Web` entry.

