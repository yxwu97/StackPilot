# P2D-04 Evidence - PMS Web

Status date: 2026-08-18

The PMS Vite server now defaults to loopback port 32102 with `strictPort` and reads `VITE_API_TARGET`. The StackPilot manifest supplies both the planned Web port on the CLI and the planned Backend target in the environment.

Six pre-existing route imports used `Edit.vue` while the real files are named `edit.vue`; their casing was corrected so strict TypeScript verification succeeds on Windows without changing page behavior.

Verification:

```text
npm run type-check -> passed
npm run build-only -> passed, 3695 modules transformed
real managed Web endpoint on fallback port 32400 -> HTTP 200
```

The production build reports existing chunk-size warnings but no errors. No maintained PMS service configuration uses port 5173.
