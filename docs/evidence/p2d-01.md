# P2D-01 Evidence - PMS Inventory

Status date: 2026-08-18

P2D-01 is verified against the real `E:\PMSystem` repository. The repository was clean before the scoped integration changes and uses remote `https://github.com/yxwu97/pmsystem.git` on branch `pmsystem-202604-v3`.

| Service | Runtime | Baseline | Readiness | External capabilities |
| --- | --- | --- | --- | --- |
| Backend | Maven, Spring Boot 3.3.7, Java 21 | port 8080 | `/actuator/health` after the P2D adaptation | MySQL 3306 and Redis 6379 remain external, already-running local dependencies |
| RAG | checked-in Windows Python venv, FastAPI/Uvicorn | port 8100 | `/health`; returns 503 while Qdrant is degraded and 200 only when Qdrant is ready | Qdrant 6333, shared service token, optional DashScope provider |
| Web | npm, Vue 3, TypeScript, Vite | preferred port 32102 | HTTP `/` | proxy target is the planned Backend URL |

The legacy `start-pmsystem.bat` launches detached windows and contains fixed lifecycle logic. StackPilot never invokes it. The script and project guidance were updated from 5173 to 32102 so no maintained PMS runtime path selects 5173.

The existing PMS local configuration remains owned by PMS. No database password, JWT value, service token, provider key, or `.env` value was copied into StackPilot source, evidence, resolved snapshots, or logs.
