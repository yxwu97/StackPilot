# StackPilot 编码与交付规则

本文规定日常实现、修改、验证和交付方式。项目事实、阶段范围和架构边界以 `AGENTS.md`、`CLAUDE.md` 及 `docs/` 下设计文档为准。

## 1. 变更原则

- 先确认任务所属 Phase、工作包、依赖和 Gate，再开始实现。
- 只修改完成当前目标所需的文件；保留用户已有修改，不顺带重构无关模块。
- 优先修复根因，不用吞错、固定延时、重复重试或状态强制覆盖掩盖问题。
- 使用结构化解析器处理 YAML、JSON、URL、路径和 SQL，不用脆弱的字符串拼接代替。
- 不编造 API、Schema 字段、数据库列、错误码、状态迁移、工具输出或测试结果。
- 禁止伪代码、空实现、永远成功的 stub 和没有 capability gate 的占位功能进入主路径。
- 除非用户明确要求，不初始化 Git，不创建提交，不推送或改写历史。

## 2. 阅读与修改范围

- 修改前完整读取目标文件，并定向检查调用方、实现方、测试和契约。
- 文本与文件检索优先使用 `rg`/`rg --files`；避免无目的全仓库内容加载。
- 公共契约或共享状态变更必须检查所有直接消费者，不能只修复单一路径。
- 手工编辑使用小范围补丁；机械格式化和代码生成使用项目固定命令。
- 不删除公共方法或兼容行为，除非需求明确且所有调用、契约、迁移和测试已同步处理。

## 3. Go 规则

- 包名简短、小写、表达职责；导出标识符必须有准确 GoDoc。
- 生产函数不超过 50 行，按单一职责拆分；状态机、事务和资源清理必须保持完整可读。
- 接口由使用方定义并保持最小；构造函数显式注入依赖，禁止可变全局单例。
- `context.Context` 是 I/O 和长耗时调用的首个参数；取消、deadline 和错误必须向下传播。
- 使用 `%w` 包装错误，使用 `errors.Is/As` 分类；只在 API/CLI 等边界映射和记录一次。
- goroutine 必须有所有者和停止机制；channel、worker、重试、日志缓冲和 SSE 客户端都必须有容量上限。
- 获取资源后立即安排释放；文件、response body、事务、ticker 和进程句柄必须覆盖失败路径。
- 使用 UTC 持久化时间，使用单调时钟部分计算耗时；测试不得依赖本机时区。
- 提交前执行 `gofmt`、`go test ./...`、`go vet ./...` 及仓库配置的静态检查。

## 4. Vue 与 TypeScript 规则

- TypeScript 使用严格模式；API DTO 显式建模，禁止把 `any` 当作跨层逃生口。
- 页面组件负责编排展示，复杂状态进入 Pinia/composable，REST/SSE 统一由 `web/src/api` 封装。
- REST 快照初始化状态，SSE 只合并版本更新；必须处理断线、重复事件、过期 cursor 和重拉快照。
- 表单校验同时覆盖客户端体验和服务端权威校验；客户端不得自行推断权限或 capability。
- 日志视图使用虚拟列表、有界缓存和稳定高度；暂停显示不能阻塞服务端流或无限积压内存。
- 使用 Element Plus 与项目已有图标体系，保持原型中的信息结构和操作语义。
- 状态必须同时包含文字/图标，不只靠颜色；对话框需处理焦点、Esc 和提交中状态。
- 新增路由、Store、API 或环境变量时，同步相关类型、测试和文档。
- 提交前执行前端格式检查、lint、type-check、组件测试和生产构建。

## 5. API、SSE 与领域规则

- 所有外部 API 使用 `/api/v1`，请求、响应、状态码和示例以 OpenAPI 为准。
- Controller/handler 只做鉴权、解析、校验、调用用例和 DTO 映射，不承载 DAG、端口或生命周期规则。
- 所有变更操作返回持久化 Operation；冲突、幂等、取消和终态遵循详细设计。
- 领域状态只能通过集中定义的合法迁移修改；非法迁移必须返回稳定领域错误。
- 错误码必须登记 HTTP 映射、用户消息、可重试性和安全 details；内部错误只暴露 `traceId`。
- 领域事件与状态更新同事务提交；SSE 通知丢失时仍能从数据库恢复。
- 日志流与领域事件流使用各自 cursor，不用文件偏移或数组索引冒充稳定游标。

## 6. SQLite 与 migration 规则

- 使用 `modernc.org/sqlite`，禁止为了本地便利引入 CGO 驱动。
- migration 文件使用单调版本号和 checksum；已合入版本只增不改。
- 写 SQL 前核对 migration、当前 schema、repository 查询和约束，不凭字段名印象实现。
- schema 变更必须在同一变更中更新 migration、repository、schema 文档和升级测试。
- 状态/事件原子性、活动实例和端口租约唯一性必须由事务及数据库约束保证。
- 禁止忽略 SQLite busy、scan、commit 或 rollback 错误；重试必须有界并可取消。
- 至少测试空库升级、上一版本升级、重复启动、checksum 不一致和并发约束。

## 7. Windows 进程与路径规则

- Process Driver 不调用业务启动脚本，不通过 API 接收任意可执行命令。
- Supervisor、Job Object、Named Pipe 和身份文件的行为以 P0-08 Spike/ADR 为准。
- 停止前核验 PID、创建时间、可执行路径、账号和协议版本；身份不匹配时拒绝终止。
- `.cmd` 使用固定解释器参数和经过测试的 Windows quoting；参数始终以数组形式跨层传递。
- 路径必须经过绝对化、canonicalize、junction/符号链接解析和工作区边界验证。
- Windows 测试覆盖空格、中文、引号、尾随反斜杠、隐藏窗口、子孙进程和崩溃恢复。
- 不允许用固定 sleep 判断 readiness；使用 process/TCP/HTTP 检查和明确 timeout/cancel。

## 8. 安全、日志与数据规则

- 默认监听回环地址；扩大监听、远程访问或权限模型前必须先完成设计与安全评审。
- Secret 和令牌只在需要时进入内存，不进入 SQLite 明文字段、日志、DTO、SSE、快照或测试附件。
- 所有日志先脱敏再持久化或分发；脱敏失败时宁可拒绝输出敏感内容。
- Web cookie、Origin、CSRF 和 CLI Bearer 是两条独立认证路径，不互相降级。
- 文件删除和覆盖属于高风险操作，执行前必须验证登记记录、规范路径、边界和文件类型。
- 日志、错误、源码、配置、API 和文档使用 UTF-8；文本文件使用 UTF-8 无 BOM 和 LF。

## 9. 测试规则

- 测试强度按风险扩大：窄改动跑定向测试，共享契约、状态机、存储或进程生命周期改动跑完整相关回归。
- 单元测试覆盖正常、边界和非法状态；集成测试使用真实 SQLite、真实 HTTP/SSE 和可控进程 fixture。
- 进程 fixture 必须可重复，不依赖 BTC 才能制造 slow-ready、异常退出、进程树、日志压力和端口竞争。
- BTC 真实 E2E 用于阶段 Gate，不能用 mock 或 fixture 声称通过真实接入验收。
- 测试不得访问用户真实数据目录，不得遗留进程、端口、临时文件或包含 Secret 的产物。
- 失败测试必须报告根因；不得通过放宽断言、增加无依据 sleep 或跳过测试使流水线变绿。

## 10. 契约与文档同步表

| 变更类型 | 同步内容 |
| --- | --- |
| REST/SSE | OpenAPI、DTO、错误码、客户端和契约测试 |
| 清单 | JSON Schema、示例、Loader/Validator 和设计文档 |
| 数据库 | 新 migration、repository、schema 说明和升级测试 |
| 状态机/Operation | 详细设计、迁移规则、事件、恢复逻辑和测试 |
| Windows 监管 | ADR、平台接口、Spike/集成证据和已知限制 |
| UI 工作流 | 路由、Store/API、组件测试、E2E 和必要的交互文档 |
| capability | `/version`、服务端能力字段、前端可用性和阶段计划 |

## 11. 完成前检查

1. 改动是否处于当前 Phase，依赖工作包是否已满足。
2. 实现、OpenAPI、Schema、migration、错误码和文档是否一致。
3. 格式化、静态检查、单元、集成、E2E 是否按风险执行并通过。
4. 是否覆盖失败、取消、超时、恢复、并发和资源清理分支。
5. 是否泄露令牌、Secret、完整命令、环境或未脱敏日志。
6. Windows 进程、文件、端口、数据库和临时目录是否无遗留。
7. `AGENTS.md` 与 `CLAUDE.md` 是否仍逐字一致。
8. 未执行的验证、环境限制和剩余风险是否在交付说明中明确列出。
