services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.3.3
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: stackpilot-p2c03-gate-only
    ports:
      - target: 18180
        published: "62777"
        host_ip: 127.0.0.1
        protocol: tcp